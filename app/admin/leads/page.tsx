import {redirect} from "next/navigation";import {getSession} from "@/lib/auth";import {db} from "@/lib/db";export const dynamic="force-dynamic";export default async function Leads(){if(!(await getSession())?.email)redirect("/admin/login");const rows=await db.lead.findMany({orderBy:{createdAt:"desc"}});return <section className="admin"><div className="container"><div className="eyebrow">CRM</div><h1>Leads</h1><div className="card"><table className="table"><thead><tr><th>Name</th><th>Email</th><th>Company</th><th>Service</th><th>Status</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.name}</td><td>{x.email}</td><td>{x.company||"—"}</td><td>{x.service||"—"}</td><td>{x.status}</td></tr>)}</tbody></table>{!rows.length&&<p>No leads yet.</p>}</div></div></section>}
