import {redirect} from "next/navigation";import {getSession} from "@/lib/auth";import {db} from "@/lib/db";export const dynamic="force-dynamic";export default async function Projects(){if(!(await getSession())?.email)redirect("/admin/login");const rows=await db.project.findMany({orderBy:{createdAt:"desc"}});return <section className="admin"><div className="container"><div className="eyebrow">CMS</div><h1>Projects</h1><div className="card"><table className="table"><thead><tr><th>Title</th><th>Category</th><th>Featured</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.title}</td><td>{x.category}</td><td>{x.featured?"Yes":"No"}</td></tr>)}</tbody></table></div></div></section>}
