import {redirect} from "next/navigation";import {getSession} from "@/lib/auth";import {db} from "@/lib/db";export const dynamic="force-dynamic";export default async function Blog(){if(!(await getSession())?.email)redirect("/admin/login");const rows=await db.blogPost.findMany({orderBy:{createdAt:"desc"}});return <section className="admin"><div className="container"><div className="eyebrow">CMS</div><h1>Blog</h1><div className="card"><table className="table"><thead><tr><th>Title</th><th>Published</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.title}</td><td>{x.published?"Yes":"Draft"}</td></tr>)}</tbody></table></div></div></section>}
