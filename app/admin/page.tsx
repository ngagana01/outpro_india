import {redirect} from "next/navigation";import Link from "next/link";import {getSession} from "@/lib/auth";import {db} from "@/lib/db";export const dynamic="force-dynamic";export default async function Admin(){if(!(await getSession())?.email)redirect("/admin/login");const [leads,projects,services,posts]=await Promise.all([db.lead.count(),db.project.count(),db.service.count(),db.blogPost.count()]);return <section className="admin"><div className="container"><div className="eyebrow">Management Console</div><h1>Admin Dashboard</h1><div className="grid">{[["Leads",leads,"/admin/leads"],["Projects",projects,"/admin/projects"],["Services",services,"/admin/services"],["Blog Posts",posts,"/admin/blog"]].map(x=><Link className="card" href={x[2]} key={x[0]}><div className="eyebrow">{x[0]}</div><h2>{x[1]}</h2><span className="green">Manage →</span></Link>)}</div></div></section>}
