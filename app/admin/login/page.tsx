"use client";import {useState} from "react";import {useRouter} from "next/navigation";export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});if(r.ok)router.push("/admin");else setError("Invalid email or password.")}return <section className="admin"><div className="container"><div className="card" style={{maxWidth:480,margin:"auto"}}><div className="eyebrow">Admin</div><h1>Sign in</h1><form className="form2" onSubmit={submit}><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Admin email" required/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" required/><button className="btn primary">Sign In</button>{error&&<div className="error">{error}</div>}</form><p>Default credentials are in .env.</p></div></div></section>}
