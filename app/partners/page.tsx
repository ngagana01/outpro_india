import {db} from "@/lib/db";export const dynamic="force-dynamic";export default async function Partners(){const partners=await db.partner.findMany({orderBy:{createdAt:"desc"}});return <><section className="page"><div className="container"><div className="eyebrow">Partner Program</div><h1>Grow together through strategic partnerships.</h1><p>Technology, delivery and business partnerships designed for long-term value.</p></div></section><section className="section"><div className="container"><div className="grid">{partners.length?partners.map(p=><div className="card" key={p.id}><div className="tag">PARTNER</div><h3>{p.name}</h3><p>{p.description}</p>{p.website&&<a className="green" href={p.website}>Visit partner →</a>}</div>):<div className="card"><h3>Partner network coming soon</h3><p>Partner records can be managed from the database.</p></div>}</div></div></section></>}
