import {NextResponse} from "next/server";import {db} from "@/lib/db";import {leadSchema} from "@/lib/validation";export async function POST(req:Request){try{const parsed=leadSchema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:"Invalid form data"},{status:400});const lead=await db.lead.create({data:parsed.data});return NextResponse.json({ok:true,id:lead.id})}catch{return NextResponse.json({error:"Server error"},{status:500})}}
