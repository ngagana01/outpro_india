import {db} from "@/lib/db";export const dynamic="force-dynamic";export default async function Blog(){const posts=await db.blogPost.findMany({where:{published:true},orderBy:{createdAt:"desc"}});return <><section className="page"><div className="container"><div className="eyebrow">Insights</div><h1>Ideas, technology and growth.</h1><p>Thought leadership and practical insights for modern businesses.</p></div></section><section className="section"><div className="container"><div className="grid">{posts.length?posts.map(p=><article className="card" key={p.id}><div className="tag">ARTICLE</div><h3>{p.title}</h3><p>{p.excerpt||p.content.slice(0,160)}</p></article>):<div className="card"><h3>Blog coming soon</h3><p>Publish your first article from the admin database.</p></div>}</div></div></section></>}
