import {db} from "@/lib/db";export const dynamic="force-dynamic";export default async function Careers(){const jobs=await db.career.findMany({where:{active:true},orderBy:{createdAt:"desc"}});return <><section className="page"><div className="container"><div className="eyebrow">Careers</div><h1>Build meaningful digital work with us.</h1><p>Join a team that values craft, ownership and continuous learning.</p></div></section><section className="section"><div className="container"><div className="grid">{jobs.length?jobs.map(j=><div className="card" key={j.id}><div className="tag">{j.type} · {j.location}</div><h3>{j.title}</h3><p>{j.description}</p><a className="green" href="mailto:careers@outpro.india?subject=Application">Apply →</a></div>):<div className="card"><h3>No open positions</h3><p>Check back soon for new opportunities.</p></div>}</div></div></section></>}
